{
  "name": "puhelinluettelo-backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "@powershell Remove-Item -Recurse -Force dist && cd ../../osa2/puhelinluettelo && npm run build && @powershell Copy-Item dist -Recurse ../../osa3/puhelinluettelobackend",
    "deploy:full": "npm run build:ui && cd ../.. && git add . && git commit -m uibuild && git push -u origin master"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "morgan": "^1.10.1"
  }
}
